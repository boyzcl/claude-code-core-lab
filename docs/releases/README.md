# Release Notes

这里放公开发布说明和 release note。

当前入口：

- [v0.1 Learning Preview](v0.1-learning-preview.md)

Release note 不替代 `CURRENT_STATE.md`、`docs/records/` 或 `src/**/*.verify.mjs`。它只负责把某个公开版本的课程、Lab、Core、实践层、验证结果和边界浓缩成发布说明。
